@using MoGYSD.Business.Persistence
@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo($"/Account/Login", forceLoad: true); //returnUrl={Uri.EscapeDataString(NavigationManager.Uri)}, forceLoad: true
    }
}
